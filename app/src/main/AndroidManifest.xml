<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecommerce"
        tools:targetApi="31">
        <activity
            android:name=".ui.lets_start_cards.lets_start_cards"
            android:exported="false" />
        <activity
            android:name=".ui.setup_new_password.setup_new_password"
            android:exported="false" />
        <activity
            android:name=".ui.smsPasswordRecovery.Sms_password_recovery"
            android:exported="false" />
        <activity
            android:name=".ui.passwordrecovery.password_recovery"
            android:exported="false" />
        <activity
            android:name=".ui.login.EnterPassword"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginScreen"
            android:exported="false"
            android:theme="@style/Theme.Ecommerce" />
        <activity
            android:name=".ui.letsStarted.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Ecommerce">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.createAccount.CreateAccount"
            android:exported="false"
            android:theme="@style/Theme.Ecommerce" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>